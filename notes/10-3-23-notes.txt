;; This buffer is for text that is not saved, and for Lisp evaluation.
;; To create a file, visit it with C-x C-f and enter text in its buffer.

center configuration

more steps

more frequent resampling

could also maybe log histograms of the propeller twists -- would see if only those states that are on the edge are causing problems.

should also log running average of proppeller twist to see if we are getting a good enough estimate....


could also just be the energy...
