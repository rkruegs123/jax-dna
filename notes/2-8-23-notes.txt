;; This buffer is for text that is not saved, and for Lisp evaluation.
;; To create a file, visit it with C-x C-f and enter text in its buffer.

use the known SI parameters and comput ethe curvce (extensions). plot it. see if it's per bp or absolute. if absolute, we are we so off?
- in fact, can try both the correct and the incorrect expression

ah. we are also initializing parameters in A when we really want to be in nm...


START AVERAGING TOO SOON, SO WE UNDERESTIMATE!! Could even start not flipped...
